export const getTitle = (val?: string) => {
	return val ?? 'Jira Kanban Board with SvelteKit, Firebase Storage and Upstash'
}

export const getDescription = (val?: string) => {
	return val ?? 'Experience the flexibility and control of hosting your own Jira instance, empowered by SvelteKit, Firebase Storage and Upstash.'
}
